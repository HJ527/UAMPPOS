export enum ButtonStyle {
  GHOST = 'ghost',
  NORMAL = 'normal',
  OUTLINE = 'outline',
  PRIMARY = 'primary',
  'PRIMARY-ALPHA' = 'primary-alpha',
}

export enum ButtonIconDirection {
  LEFT = 'left',
  TOP = 'top',
  RIGHT = 'right',
  BOTTOM = 'bottom',
}
